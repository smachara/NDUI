{% spaceless %}
    {% for key in nodes|keys %}
    {{ key }}:
        {% for a in nodes[key].networkfunction.attributes %}
            {{ nodes[key].lvl | }}- {{ a.name }}: {{ a.value }}
        {% endfor %}
    {% endfor %}
{% endspaceless %}